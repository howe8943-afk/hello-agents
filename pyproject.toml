[project]
name = "hello-agent"
version = "0.1.0"
description = "Hello-Agents learning notes and experiments"
readme = "README.md"
authors = [
    { name = "WenHao YAN", email = "howe8943@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]

[project.scripts]
hello-agent = "hello_agent:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
